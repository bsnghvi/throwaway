// Bloom module root. Concrete types live in DesignSystem/, Components/, Models/,
// Onboarding/, and Home/. This file just anchors the module so an empty
// scaffold still compiles cleanly.
public enum Bloom {
    /// Semantic version of the Bloom library bundle.
    public static let version = "0.1.0"
}
